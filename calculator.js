class Calculator {
  sum = (num1, num2) => {
    return num1 + num2;
  };

  subs = (num1, num2) => {
    return num1 - num2;
  };

  divide = (num1, num2) => {
    return num1 / num2;
  };

  multiply = (num1, num2) => {
    return num1 * num2;
  };
}

module.exports = Calculator;
